onelake:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('ONELAKE_DUCKDB', '../data/warehouse/onelake.duckdb') }}"
      threads: 4
      schema: main
      settings:
        timezone: UTC
